using System;

public class Obj
{
    public static bool IsOnlyASubclass(Type derivedType, Type baseType)
    {
        if(obj.IsSubclassOf(baseType))
        return true;
        else
        return false;
    }
}