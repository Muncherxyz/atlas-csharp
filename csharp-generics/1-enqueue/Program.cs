using System;

/// <summary>
/// Class named Queue
/// </summary>
class Queue<T>
{
    /// <summary>
    /// method that returns the queues type.
    /// </summary>
    public Type CheckType()
    {
        return typeof(T);
    }

    public class Node
    {
        T value = null;
        Node next = null;

        public nodeNew(T Value)
        {
            this.value = value;
        }

        Node head;
        Node tail;
        int count;

        public Enqueue()
        {
            Node newNode = new Node();
            if(Queue == null)


            
        }

        public Count()
        {
            return count;
        }
    }
}